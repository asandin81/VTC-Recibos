import 'package:flutter/material.dart';

class NuevoRecibo extends StatelessWidget {
  const NuevoRecibo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('data');
  }
}
